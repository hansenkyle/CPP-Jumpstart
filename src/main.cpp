/**
 * @file main.cpp
 * @brief Entry point for the `myproject` executable.
 */

#include "module.h"

/**
 * @brief Program entry point.
 * @return Exit status code; `0` on success.
 */
int main() { return 0; }
