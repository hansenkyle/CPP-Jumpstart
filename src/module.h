/**
 * @file module.h
 * @brief Core module for myproject.
 *
 * Public declarations for the `myproject_core` library live here. This
 * header is included by both the `myproject` executable and the unit
 * tests, so anything declared here is shared across the whole project.
 */

#ifndef MODULE_H
#define MODULE_H

#endif // MODULE_H
